<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="build-common" default="dist">
    <description>
        simple example build file
    </description>
  <property name="src" location="src"/>
  <property name="build" location="build"/>
  <property name="dist"  location="dist"/>
  <property name="docs"  location="docs"/>

  <target name="init"
        description="initialize">
    <echo message="${ant.project.name} - build-common.init"/>
    <!-- Create the time stamp -->
    <tstamp/>
    <mkdir dir="${build}"/>
  </target>

  <target name="compile" depends="init"
        description="compile the source ">
    <echo message="${ant.project.name} - build-common.compile"/>
    <javac srcdir="${src}" destdir="${build}" debug="yes"/>
  </target>

  <target name="dist" depends="compile"
        description="generate the distribution">
    <echo message="${ant.project.name} - build-common.dist"/>
    <mkdir dir="${dist}"/>
    <jar jarfile="${dist}/IWB-${ant.project.name}-${DSTAMP}.jar" basedir="${build}"/>
  </target>

  <target name="docs" depends="compile"
        description="generate the documentation">
    <javadoc packagenames="org.iwb*"
             sourcepath="src"
             defaultexcludes="yes"
             destdir="docs"
	     private="yes"
             author="true"
             version="true"
             use="true"
             windowtitle="Information Without Borders - ${ant.project.name}">
      <fileset dir="${src}" defaultexcludes="yes">
	<include name="**.java"/>
      </fileset>
      <doctitle><![CDATA[<h1>Test</h1>]]></doctitle>
      <bottom><![CDATA[<i>Copyright &#169; 2006 <a href="http://informationwithoutborders.endymion.com">Information Without Borders</a>. All Rights Reserved.</i>]]></bottom>
      <link href="http://developer.java.sun.com/developer/products/xml/docs/api/"/>
      <group title="IWB" packages="org.iwb*"/>
    </javadoc>
  </target>

  <target name="clean"
        description="clean up" >
    <echo message="${ant.project.name} - build-common.clean"/>
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
    <delete dir="${docs}"/>
  </target>

  <target name="dist.dependencies">
      <antcall target="depend.${ant.project.name}"/>
  </target>
</project>
